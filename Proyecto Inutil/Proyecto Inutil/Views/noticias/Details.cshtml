@model Proyecto_Inutil.Models.EtNot

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>noticia</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.not.fecha)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.not.fecha)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.not.titulo)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.not.titulo)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.not.contenido)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.not.contenido)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.not.usuario.nombre)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.not.usuario.nombre)
        </dd>

    </dl>
    <hr />
    <div>
        <h3>Etiquetas</h3>
        @foreach (var item in Model.et)
        {
        <p>@Html.ActionLink(item.nombre, "Details", "etiquetas", new { id = item.id }, null)</p>
        }
    </div>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.not.id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
